@model IEnumerable<PepakDigital.Hanacaraka>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Aksara)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GambarURL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Aksara)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GambarURL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HanacarakaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HanacarakaId })
        </td>
    </tr>
}

</table>
